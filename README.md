# debian-13-tricks

Tools and configs for Debian 13 (trixie).

- `apt-sources/` — enable/disable any installed apt source (system-wide tool)
- `sources/` — interactive setup of the trixie apt sources
- `setups/` — app setups (deb-get, ...)
